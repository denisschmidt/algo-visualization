### Commands
```
pyenv install --list - Get all available and installable versions of Python
pyenv local 3.8.7

poetry shell
poetry new <project name>
poetry install - Install and activate the virtual environment
```
